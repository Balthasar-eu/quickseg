# Quickseg

Quickseg is a copy number segmentation algorithm. This tool implements it. It uses binned integer read depth as input. You can generate appropriate files with mosdepth or samtools.

You can run the tool with:

```
quickseg [OPTIONS] --input <FILE> --output <FILE>
```

